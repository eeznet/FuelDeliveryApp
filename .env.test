# Node Environment
NODE_ENV=test

# Server Port
PORT=4000

# MySQL Database Configuration for Testing
DB_HOST=localhost
DB_USER=testuser
DB_PASSWORD=testpass
DB_NAME=testdb

# MongoDB URI for Testing (Could be an in-memory MongoDB or test database)
MONGO_URI=mongodb://localhost/test_fuel_delivery_app

# JWT Secret for testing environment
JWT_SECRET=testsecret

# Email Service Configuration for testing (can be a mock or test service)
EMAIL_USER=test@example.com
EMAIL_PASSWORD=testpassword

# Logging Level (set to 'silent' to avoid log clutter in tests)
LOG_LEVEL=silent
