<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Invoice - #<%= invoice._id %> | Fuel Delivery App</title>
    
    <meta name="description" content="Invoice details for Fuel Delivery App, including fuel type, delivery amount, and pricing.">
    <meta property="og:title" content="Invoice - #<%= invoice._id %> | Fuel Delivery App">
    <meta property="og:description" content="View and print your invoice for fuel delivery.">
    <meta property="og:image" content="/public/images/og-image.png">
    <meta property="og:url" content="https://www.yourapp.com/invoice/<%= invoice._id %>">
    
    <link rel="stylesheet" href="/public/style.css">
    <link rel="icon" type="image/png" href="/public/favicon-32x32.png">
</head>
<body>
    <div class="container">
        <h1>Invoice #<%= invoice._id %></h1>

        <!-- Display invoice details -->
        <table>
            <caption><strong>Invoice Details</strong></caption>
            <tbody>
                <tr>
                    <th scope="row">Invoice Number</th>
                    <td><%= invoice._id %></td>
                </tr>
                <tr>
                    <th scope="row">Fuel Type</th>
                    <td><%= invoice.fuelType || 'N/A' %></td>
                </tr>
                <tr>
                    <th scope="row">Liters Delivered</th>
                    <td><%= invoice.litersDelivered || '0' %></td>
                </tr>
                <tr>
                    <th scope="row">Price per Liter</th>
                    <td>$<%= invoice.pricePerLiter?.toFixed(2) || '0.00' %></td>
                </tr>
                <tr>
                    <th scope="row">Total Amount</th>
                    <td>$<%= (invoice.litersDelivered * invoice.pricePerLiter).toFixed(2) || '0.00' %></td>
                </tr>
                <tr>
                    <th scope="row">Status</th>
                    <td><%= invoice.status || 'Pending' %></td>
                </tr>
                <tr>
                    <th scope="row">Delivery Address</th>
                    <td><%= invoice.deliveryAddress || 'Not provided' %></td>
                </tr>
                <tr>
                    <th scope="row">Client</th>
                    <td><%= invoice.client?.name || 'Unknown' %></td>
                </tr>
            </tbody>
        </table>

        <!-- Print & Back Buttons -->
        <div class="button-container">
            <button onclick="window.print()" class="button" aria-label="Print this invoice">Print Invoice</button>
            <a href="/dashboard" class="button" aria-label="Return to Dashboard">Back to Dashboard</a>
        </div>
    </div>

    <!-- Footer -->
    <footer>
        <p>
            Web-based application developed and managed by 
            <a href="https://www.eeznet.com" target="_blank" rel="noopener noreferrer">
                EEZNET
            </a>
        </p>
    </footer>
</body>
</html>
